#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/node$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/node$exe"  "$basedir/../amplify-app/bin/amplify-app" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../amplify-app/bin/amplify-app" $args
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "node$exe"  "$basedir/../amplify-app/bin/amplify-app" $args
  } else {
    & "node$exe"  "$basedir/../amplify-app/bin/amplify-app" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
