import { DiffRule, ProjectRule } from 'graphql-transformer-core';
export declare type SanityCheckRules = {
    diffRules: DiffRule[];
    projectRules: ProjectRule[];
};
export declare class GraphQLSanityCheck {
    private readonly rules;
    private readonly rootStackFileName;
    private readonly currentCloudBackendDir;
    private readonly localStateDir;
    constructor(rules: SanityCheckRules, rootStackFileName: string, currentCloudBackendDir: string, localStateDir: string);
    validate: () => Promise<void>;
}
//# sourceMappingURL=sanity-check.d.ts.map